--!strict

local WinsConfig = {
	Debug = true,

	TagName = "Wins",
	TouchPartName = "TouchPart",
	WinsQuantityAttribute = "WinsQuantity",

	TouchDebounceSeconds = 1,
	SpawnTeleportOffset = Vector3.new(0, 5, 0),
}

return WinsConfig
